import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import {Whitam} from '../utils';

const Cart = () => {
  return (
    <View>
      <Text style={{color: Whitam}}>Cart</Text>
    </View>
  );
};

export default Cart;

const styles = StyleSheet.create({});
