import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import {Whitam} from '../utils';

const Profile = () => {
  return (
    <View>
      <Text style={{color: Whitam}}>Profile</Text>
    </View>
  );
};

export default Profile;

const styles = StyleSheet.create({});
